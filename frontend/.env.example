# ── SUNDAE Frontend Environment Variables ────────────────────────
# Copy this file to .env and fill in the values.
#   cp .env.example .env

# FastAPI Backend URL
VITE_API_BASE_URL=http://localhost:8000

# Supabase (Self-Hosted)
# หาได้จาก: Supabase Dashboard → Settings → API
VITE_SUPABASE_URL=https://<your-project-id>.supabase.co
VITE_SUPABASE_ANON_KEY=<your-publishable-anon-key>

# App URL — ใช้สำหรับ Supabase redirect (reset password)
# ใน production ตั้งเป็น domain จริง เช่น https://app.sundae.example.com
VITE_APP_URL=http://localhost:5173

# Defaults (Optional) — ใช้ถ้ายังไม่ login
# VITE_DEFAULT_BOT_ID=<uuid-ของ-bot>
# VITE_DEFAULT_ORG_ID=<uuid-ของ-organization>
